services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-portunusmcp}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-portunusmcp}
      POSTGRES_DB: ${POSTGRES_DB:-portunusmcp}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-portunusmcp} -d ${POSTGRES_DB:-portunusmcp}"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:7
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  gateway:
    build: .
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://portunusmcp:portunusmcp@postgres:5432/portunusmcp}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379/0}
      # Upstream stdio commands live in the policy's `servers:` block (item 35),
      # spawned per session inside this container.
      POLICY_FILE: ${POLICY_FILE:-policies/example-policy.yaml}
      # Signed-identity secret for the demo policy's ci-agent (item 34): the YAML
      # names this var; the value never enters the policy file or the repo.
      PORTUNUSMCP_DEMO_SIGNING_SECRET: ${PORTUNUSMCP_DEMO_SIGNING_SECRET:-}
      # Metrics default to loopback (config.py) so a bare-host run doesn't expose
      # identity ids / tool names. Here the port is unpublished, so the compose
      # network is the boundary and Prometheus needs to reach it.
      METRICS_HOST: "0.0.0.0"
    volumes:
      # Host-editable policies: edit + `docker kill -s HUP portunusmcp-gateway-1` to reload.
      # Read-only so a compromised gateway can't persist a policy backdoor; only the
      # revisions submount is writable, for activation snapshots (item 19).
      - ./policies:/app/policies:ro
      - ./policies/revisions:/app/policies/revisions
      # Audit signing keypair: mint once via `python scripts/generate_signing_key.py`.
      - ./secrets:/app/secrets:ro
      # Rogue-demo mutation state, shared with the rogue admin service (item 14). A bind
      # mount so run_demo.py can reset/watch it from the host; gitignored.
      - ./.rogue-state:/rogue-state
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy

  # Detective control (§5): its downtime never blocks gateway traffic.
  verifier:
    build: .
    command: python scripts/audit_verifier_daemon.py
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+asyncpg://portunusmcp:portunusmcp@postgres:5432/portunusmcp}
      VERIFY_INTERVAL_SECONDS: ${VERIFY_INTERVAL_SECONDS:-60}
      METRICS_PORT: "9101"
      METRICS_HOST: "0.0.0.0"
    volumes:
      # Only the public key is read; the private key never leaves the gateway process.
      - ./secrets:/app/secrets:ro
    depends_on:
      postgres:
        condition: service_healthy

  # Rogue-demo admin endpoint (item 14): mutation is a real, operator-triggered call —
  # curl -X POST localhost:9800/_admin/apply_mutation — never a timer. The rogue MCP
  # server itself runs as per-session stdio subprocesses inside the gateway container
  # (the demo policy's `servers:` block registers it, see scripts/run_demo.py);
  # this service only hosts the mutation endpoint, sharing state via the bind mount.
  rogue:
    build: .
    command: python sample_target/rogue_server.py --admin --port 9800 --state /rogue-state/state.json
    volumes:
      - ./.rogue-state:/rogue-state
    ports:
      - "9800:9800"

  # Observability (§7, item 25): opt-in via `docker compose --profile monitoring up -d`.
  # Prometheus scrapes gateway:9100 / verifier:9101 over the compose network only —
  # those ports are deliberately not published (metric labels carry identity ids and
  # tool names); the §7 posture is unauthenticated-but-internal.
  prometheus:
    image: prom/prometheus:v3.4.1
    profiles: ["monitoring"]
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:12.0.2
    profiles: ["monitoring"]
    environment:
      # Local dev dashboard — anonymous read access, no login wall.
      GF_AUTH_ANONYMOUS_ENABLED: "true"
      GF_AUTH_ANONYMOUS_ORG_ROLE: Admin
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    ports:
      - "3000:3000"
    depends_on:
      - prometheus

volumes:
  pgdata:
